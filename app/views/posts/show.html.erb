<h1><%= @post.title %></h1>
<p><%= @post.body %></p>
<% @comments.each do |comment| %>
  <div>
      <p><%= comment.username %></p>
      <p><%= comment.created_at.strftime("%d %b. %Y") %></p>
      <p><%= comment.body %></p>
  </div>
<% end %>
